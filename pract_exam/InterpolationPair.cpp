#include "InterpolationPair.hpp"

InterpolationPair::InterpolationPair(char* key, char* value) : SimplePair(key, value)
{
    int index;
    bool foundParameter = false;
    int help;
    while(key[index])
    {
        
        index++;
    }
}

